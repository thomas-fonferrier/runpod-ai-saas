apiVersion: v1
kind: Service
metadata:
  name: flux-worker-svc
  namespace: ai-workers
  labels:
    app: flux-worker
spec:
  selector:
    app: flux-worker
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
