apiVersion: v1
kind: Service
metadata:
  name: ltxvideo-worker-svc
  namespace: ai-workers
  labels:
    app: ltxvideo-worker
spec:
  selector:
    app: ltxvideo-worker
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
