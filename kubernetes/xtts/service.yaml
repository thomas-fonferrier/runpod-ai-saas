apiVersion: v1
kind: Service
metadata:
  name: xtts-worker-svc
  namespace: ai-workers
  labels:
    app: xtts-worker
spec:
  selector:
    app: xtts-worker
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
